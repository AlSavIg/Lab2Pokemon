package Moves;
import ru.ifmo.se.pokemon.*;

public class IceBeam extends SpecialMove {
    public IceBeam(final Type type,
                        double pow,
                        double acc) {
        super(type, pow, acc);
    }
}
