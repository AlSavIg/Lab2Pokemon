package Moves;
import ru.ifmo.se.pokemon.*;

public class Psychic extends SpecialMove {
    public Psychic(final Type type,
                        double pow,
                        double acc) {
        super(type, pow, acc);
    }
}
