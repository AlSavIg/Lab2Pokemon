package Moves;
import ru.ifmo.se.pokemon.*;

public class Confide extends PhysicalMove {
    public Confide(final Type type,
                   double pow,
                   double acc) {
        super(type, pow, acc);
    }
}
