package Moves;
import ru.ifmo.se.pokemon.*;

public class PoisonJab extends SpecialMove {
    public PoisonJab(final Type type,
                     double pow,
                     double acc) {
        super(type, pow, acc);
    }
}
